import type { Meta } from '@storybook/react';
import Index from './index';

const Story: Meta<typeof Index> = {
  component: Index,
  title: 'Index',
};
export default Story;

export const Primary = {
  args: {},
};
